#include "main.h"

/**
 * _abs - calculates the absolute value of a number
 * @x: accepts integer as an input
 * Return: 0
 */

int _abs(int x)
{
	if (x >= 0)
		return (x);
	else
		return (-1 * x);
}
