#include "main.h"

/**
 * set_string - sets pointer to char
 * @s: double pointer which value is assigned
 * @to: char that a value is assigned to
 */

void set_string(char **s, char *to)
{
	*s = to;
}
