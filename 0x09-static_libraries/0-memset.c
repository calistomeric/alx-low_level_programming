#include "main.h"

/**
 * _memset - checks if it is upper
 * @s: input string
 * @b: input character
 * @n: input integer
 * Return: anything
 */

char *_memset(char *s, char b, unsigned int n)
{
	return (0);
}
