#include <stdio.h>
/**
 * main - A program to print the base ten of single digit numbers
 * Return: 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
